Notes:
    (1) Disk creation
            qemu-img create -f raw tinycore.raw 12M
    (2) OS Installation -- Used virt-manager GUI
            sudo virt-manager
    (3) Run disk_analyzer
    (4) Run inference_engine with VM

Requirements:

    (1) libhiredis for Redis communication 
